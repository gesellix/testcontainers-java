name: Release Drafter

on:
  push:
    branches:
      - master

jobs:
  update_release_draft:
    runs-on: ubuntu-latest
    steps:
      - uses: release-drafter/release-drafter@1eb031648ba18ffa376d3e4f2c33a6364a5a4dc2 #v5.13.0
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
